<% @title = "Edit Backdrop \'#{@backdrop.name}\'" %>

<div class="page-header">
  <h1>
    <%= @backdrop.name %>
    <small class="pull-right">
      <span class="btn-group">
        <%= link_to @backdrop, :method => :delete, :data => { confirm: "The backdrop \"#{@backdrop.name}\" will be removed?" }, :class => "btn btn-danger" do %>
          <span class="glyphicon glyphicon-trash"></span>
        <% end %>
      </span>
    </small>
    <br>
    <small>Backdrop</small>
    <%= render 'tags' %>
    <%= render 'stages' %>
  </h1>
</div>

<%= render 'form' %>
