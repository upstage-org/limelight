<% @title = "Edit Avatar \'#{@avatar.name}\'" %>

<div class="page-header">
  <h1>
    <%= @avatar.name %>
    <small class="pull-right">
      <span class="btn-group">
        <%= link_to @avatar, :method => :delete, :data => { confirm: "The avatar \"#{@avatar.name}\" will be removed?" }, :class => "btn btn-danger" do %>
          <span class="glyphicon glyphicon-trash"></span>
        <% end %>
      </span>
    </small>
    <br>
    <small>Avatar</small>
    <%= render 'tags' %>
  </h1>
</div>

<%= render 'form' %>
