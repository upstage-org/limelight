<div class="page-header">
  <h1>
    <%= @role.name %>
    <small class="pull-right">
      <span class="btn-group">
        <%= link_to 'Edit', edit_role_path(@role), :class => "btn btn-default" %>
        <%= link_to 'Delete', @role, :method => :delete, :data => { confirm: "The role \"#{@role.name}\" will be removed. Are you sure?", title: "Confirmation" }, :class => "btn btn-danger" %>
      </span>
    </small>
  </h1>
</div>
