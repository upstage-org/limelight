<div class="page-header">
  <h1>
    <%= @tag.name %>
    <small class="pull-right">
      <span class="btn-group">
        <%= link_to 'Delete', @tag, :method => :delete, :data => { confirm: "The tag \"#{@tag.name}\" will be removed. Are you sure?", title: "Confirmation" }, :class => "btn btn-danger" %>
      </span>
    </small>
  </h1>
</div>
